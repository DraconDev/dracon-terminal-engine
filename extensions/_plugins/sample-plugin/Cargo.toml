[package]
name = "sample-plugin"
version = "0.1.0"
edition = "2021"
authors = ["Dracon Terminal Engine Team"]
description = "Sample plugin demonstrating the plugin system"
license = "MIT OR Apache-2.0"

[dependencies]
dracon-terminal-engine = { path = "../../../.." }